<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>BookStore App</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f2f2f2;
      }
      .card-img-top {
        object-fit: cover;
        height: 300px;
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand fw-bold" href="/">BookStore </a>
        <a class="btn btn-outline-light" href="/add">+ Add Book</a>
      </div>
    </nav>
    <div class="container mt-5">
      <div class="row g-5">
        <div class="col-md-6">
          <img
            src="/uploads/<%= book.image %>"
            class="img-fluid rounded shadow"
          />
        </div>
        <div class="col-md-6">
          <h2><%= book.name %></h2>
          <p>
            <strong>Release:</strong> <%= book.releaseDate.toDateString() %>
          </p>
          <p><strong>Genre:</strong> <%= book.genre %></p>
          <p><strong>Description:</strong> <%= book.description %></p>
          <p><strong>Rating:</strong> ⭐ <%= book.rating %></p>
          <p><strong>Review:</strong> <%= book.review %></p>
          <p><strong>Author:</strong> <%= book.author %></p>
          <a href="/" class="btn btn-outline-secondary mt-3">← Back to Home</a>
        </div>
      </div>
    </div>
    <footer class="bg-dark text-white text-center py-3 mt-5">
      <div class="container">
        <small
          >© 2025 BookStore. Made with ❤️ using Node.js, EJS, and
          Bootstrap.</small
        >
      </div>
    </footer>
  </body>
</html>
